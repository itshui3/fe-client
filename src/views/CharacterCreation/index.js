import React from 'react';
import Layout from '../../common/Layout';
import { Button, Flex, FormLabel, Input, Stack, Text } from '@chakra-ui/core';

export default function CharacterCreation() {
  return (
    <Layout minH="100vh" maxW="100vw">
      <Stack minH="92vh" justify="center" align="center"></Stack>
    </Layout>
  );
}
